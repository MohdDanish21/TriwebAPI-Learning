const str= "Hi its a animal";
const lowerStr=str.toLowerCase();
console(lowerStr);


const upperStr=str.toUpperCase();
console(upperStr);

const trimStr=str.trim();
console.log(trimStr.length);


//Multiline and embedded

const str1= "how how are you.\its raining today";
console.log(str1);

